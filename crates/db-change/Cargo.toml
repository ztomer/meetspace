[package]
name = "db-change"
version = "0.1.0"
edition = "2024"

[dependencies]
meetspace-cloudsync = { workspace = true }

sqlx = { workspace = true, features = ["runtime-tokio", "sqlite"] }
tokio = { workspace = true, features = ["sync"] }

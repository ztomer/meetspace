[package]
name = "transcription-core"
version = "0.1.0"
edition = "2024"
description = ""

[features]
default = []
specta = [
  "meetspace-listener-core/specta",
  "meetspace-listener2-core/specta",
]
tauri-event = [
  "meetspace-listener-core/tauri-event",
  "meetspace-listener2-core/tauri-event",
]

[dependencies]
meetspace-listener-core = { workspace = true }
meetspace-listener2-core = { workspace = true }

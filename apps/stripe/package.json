{
  "name": "@meetspace/stripe",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenvx run --ignore MISSING_ENV_FILE -f ../../.env.supabase -f ../../.env.stripe -f .env -- bun --hot src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "sh -c 'if find src -type f \\( -name \"*.test.ts\" -o -name \"*.test.tsx\" -o -name \"*.test.js\" -o -name \"*.test.jsx\" -o -name \"*.spec.ts\" -o -name \"*.spec.tsx\" -o -name \"*.spec.js\" -o -name \"*.spec.jsx\" \\) | grep -q .; then command -v bun >/dev/null 2>&1 || { echo \"bun is required to run @meetspace/stripe tests\" >&2; exit 1; }; bun test --pass-with-no-tests; else node --test; fi'"
  },
  "dependencies": {
    "@meetspace/supabase": "workspace:*",
    "@sentry/bun": "^10.48.0",
    "@supabase/stripe-sync-engine": "^0.45.0",
    "@supabase/supabase-js": "^2.103.0",
    "@t3-oss/env-core": "^0.13.11",
    "effect": "^3.21.0",
    "hono": "^4.12.12",
    "pg": "^8.20.0",
    "stripe": "^20.4.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "^1.61.0",
    "@types/bun": "^1.3.12",
    "@types/pg": "^8.20.0",
    "typescript": "^5.9.3"
  }
}
